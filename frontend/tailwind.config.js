/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',
    './src/**/*.{js,jsx}', // using JSX only
  ],
  darkMode: 'class', // Enables `dark` class usage
  theme: {
    extend: {},
  },
  plugins: [],
};
